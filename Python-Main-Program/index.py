
"""
    So, the code if __name__ == "__main__": checks whether the script is being run as the main program (i.e., not imported as a module). 
    If this condition is true, the main() function is called, which typically contains the main logic of the script.
"""
# the below lines of code implies the file is executed as an independent Python program
if __name__ == " __main__ ":
    main()